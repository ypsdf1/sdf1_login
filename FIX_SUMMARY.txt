[01:19] Web端交易三个核心问题修复
==================================================

1. 商品ID无效（ACACIA_BOAT）- 已修复
   - PHP shop.php 传 item_id="ACACIA_BOAT"（Material名称）
   - Java dispatchItemByMaterialOrId() 已支持Material匹配+ID fallback
   - displayName获取已修复：优先从ItemMeta.getDisplayName()读取，而不是直接用itemId

2. 扣款变给钱（bond_log符号反了）- 已修复
   - BondManager.deductBondsInternal(): logLegacy(player, -amount) → logLegacy(player, amount)
   - bond_log表的amount字段现在记录正数，UI根据type判断加减

3. 交易轮询慢（30-90秒）- 已修复
   - PHP shop.php 和 cdk.php 在购买/兑换成功后立即POST到Java notify_sync回调
   - Java收到notify_sync后调用requestImmediateTransactionPull()
   - 最小间隔5秒，避免过度频繁

4. 回调服务器并发问题 - 已修复
   - handleCallback()分离notify_sync和register_callback两种请求
   - notify_sync不阻塞注册回调处理

5. detail JSON解析 - 已修复
   - 支持转义引号\"的嵌套JSON解析

==================================================
下一步：
1. 用IDEA重新编译打包（Maven → Lifecycle → package）
2. 将Sdf1_login.jar复制到MC服务器plugins目录覆盖
3. 重启服务器测试
4. 验证：Web购物→立即到账、CDK兑换→余额同步、扣款方向正确
